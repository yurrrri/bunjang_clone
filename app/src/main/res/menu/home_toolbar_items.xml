<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/home_toolbar_search"
        android:enabled="true"
        android:icon="@drawable/home_search_icon"
        android:title="@null"
        android:visible="true"
        app:showAsAction="always" />

    <item
        android:id="@+id/home_toolbar_heart"
        android:enabled="true"
        android:icon="@drawable/home_heartlist_icon"
        android:title="@null"
        android:visible="true"
        app:showAsAction="always" />

    <item
        android:id="@+id/home_toolbar_alarm"
        android:checkable="true"
        android:enabled="true"
        android:icon="@drawable/home_alarm_icon"
        android:title="@null"
        android:visible="true"
        app:showAsAction="always" />

</menu>